rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow full access to all collections for testing
    // WARNING: This is insecure and should only be used for testing
    match /{document=**} {
      allow read, write: if true;
    }
  }
} 